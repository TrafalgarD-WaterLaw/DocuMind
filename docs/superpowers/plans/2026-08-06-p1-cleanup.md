# P1 清理实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 清理三件套：死路由 /api/research/quick、reasoning 事件接入深度模式、CORS 修正。

**Architecture:** 删除 research.py 中死代码；orchestrator.deep_research 在每专家完成后发 REASONING 事件（带角色中文标题的专家摘要）；main.py CORS 固定 origin。

**Tech Stack:** 同 P0（FastAPI + pytest；无 git 仓库，Commit 步骤替换为验证命令）。

**Spec:** `docs/superpowers/specs/2026-08-06-full-chain-rag-hardening-design.md` 第三节 P1

## Global Constraints

- 所有注释/文案中文；项目无 git 仓库（无提交）。
- 后端测试：`cd Backend && uv run pytest tests/ -v`（当前 41 全绿，含 test_api.py）。
- 前端构建门：`cd Frontend && npm run build`（vue-tsc 零错误）。
- API key 不得出现在新增代码/日志中。

---

### Task P1-1: 删除 /api/research/quick 死路由

**Files:**
- Modify: `Backend/src/api/research.py`

**Interfaces:**
- Produces: 移除 `_generate_quick_research` 函数与 `quick_research` 路由；`/api/research`（deep）与 mock 分支不变。

- [ ] **Step 1: 确认无引用**

Run: `cd E:/projects/DocuMind && grep -rn "research/quick" Frontend/src Backend/src Backend/tests`
Expected: 无命中（或仅 research.py 自身，删除后为零）。

- [ ] **Step 2: 删除死代码**

Modify `Backend/src/api/research.py`：删除 `_generate_quick_research`（26-47 行）与 `quick_research` 路由（144-164 行）。其余（`_generate_deep_research`、`_generate_mock_research`、`deep_research` 路由）保留。

- [ ] **Step 3: 验证**

Run:
```bash
cd E:/projects/DocuMind/Backend && uv run pytest tests/ -v
```
Expected: 41 passed（无回归）。再 `grep -rn "quick_research\|research/quick" src/` → 无命中。

---

### Task P1-2: 深度模式 reasoning 事件接入

**Files:**
- Modify: `Backend/src/services/agent/orchestrator.py`
- Test: `Backend/tests/test_orchestrator_reasoning.py`

**Interfaces:**
- Consumes: `StreamEventType.REASONING`（已存在）；`_ROLE_TITLES`（已存在）。
- Produces: `deep_research` 中每个专家 `execute` 成功后、`agent_step(done)` 事件**之前**，发出 `reasoning` 事件：`data = f"【{_ROLE_TITLES[role]}】{result[:200]}…"`（result 为空则跳过；synthesizer 完成前发一条 `reasoning` 事件 `data="【著述·综合研究】正在融合各专家观点撰写报告…"`）。
- 注意：`expert_results[task.agent] = result` 赋值在 `agent_step done` 之前、`execute` 之后——reasoning 事件插在两者之间。

- [ ] **Step 1: 写失败测试**

Create `Backend/tests/test_orchestrator_reasoning.py`:

```python
"""深度模式 reasoning 事件测试——专家完成后发出带角色标题的摘要"""
import json

import pytest

from models.response import StreamEventType
from services.agent.orchestrator import ResearchOrchestrator


class _FakeLLM:
    """按调用顺序返回：意图 JSON → 史官分析 → 著述报告"""

    def __init__(self):
        self.calls = 0

    def build_messages(self, *args, **kwargs):
        return []

    async def chat(self, messages, **kwargs):
        self.calls += 1
        if self.calls == 1:
            return json.dumps(
                {"mode": "deep", "summary": "测试", "agents": ["historian"]},
                ensure_ascii=False,
            )
        if self.calls == 2:
            return "叩鼎为商代晚期青铜礼器，器形为三足圆鼎。"
        return "## 综合报告\n\n叩鼎的史官考证结论。"

    async def chat_stream(self, messages, **kwargs):
        raise AssertionError("深度模式不走 chat_stream")


async def test_deep_research_emits_reasoning():
    orch = ResearchOrchestrator(llm=_FakeLLM(), retriever=None, knowledge=None)
    events = [ev async for ev in orch.deep_research("叩鼎是什么")]

    reasoning = [ev for ev in events if ev.type == StreamEventType.REASONING]
    # 至少史官摘要一条 + 著述提示一条
    assert len(reasoning) >= 2
    assert "史官 · 历史考证" in reasoning[0].data
    assert "叩鼎为商代晚期" in reasoning[0].data
    assert "著述" in reasoning[-1].data

    # reasoning 事件位于该专家 agent_step(done) 之前
    kinds = [ev.type for ev in events]
    first_done = kinds.index(StreamEventType.AGENT_STEP)  # coordinator running
    # 找到史官 done 的位置
    agent_steps = [ev for ev in events if ev.type == StreamEventType.AGENT_STEP]
    done_pos = events.index(agent_steps[2])  # coordinator running, historian running, historian done
    reasoning_pos = events.index(reasoning[0])
    assert reasoning_pos < done_pos
```

- [ ] **Step 2: 运行确认失败**

Run: `cd E:/projects/DocuMind/Backend && uv run pytest tests/test_orchestrator_reasoning.py -v`
Expected: FAIL（无 reasoning 事件）。

- [ ] **Step 3: 实现**

Modify `Backend/src/services/agent/orchestrator.py` 的 `deep_research` 专家循环内：

1) `result = await expert.execute(task.query, task.context)` 之后、`expert_results[task.agent] = result` 之前插入：

```python
                # reasoning 事件：专家研究摘要（前端"推理过程"展示）
                if result and result.strip():
                    yield StreamEvent(
                        type=StreamEventType.REASONING,
                        data=f"【{_ROLE_TITLES.get(task.agent, task.agent)}】"
                             f"{result.strip()[:200]}…",
                        timestamp=time.time(),
                    )
                    await asyncio.sleep(0)
```

2) 在「4. 著述融合」的 `yield self._make_agent_step(AgentRole.SYNTHESIZER, "running", ...)` 之前插入：

```python
        yield StreamEvent(
            type=StreamEventType.REASONING,
            data="【著述 · 综合研究】正在融合各专家观点撰写报告…",
            timestamp=time.time(),
        )
        await asyncio.sleep(0)
```

- [ ] **Step 4: 运行确认通过**

Run: `cd E:/projects/DocuMind/Backend && uv run pytest tests/test_orchestrator_reasoning.py -v`
Expected: 1 passed。

- [ ] **Step 5: 全量回归**

Run: `cd E:/projects/DocuMind/Backend && uv run pytest tests/ -v`
Expected: 42 passed。

---

### Task P1-3: CORS 固定 origin

**Files:**
- Modify: `Backend/src/main.py`

**Interfaces:**
- Produces: `allow_origins=["http://localhost:5173", "http://192.168.137.1:5173"]`，`allow_credentials=False`（删除该参数或显式 False）；`allow_methods`/`allow_headers` 保持 `["*"]`。

- [ ] **Step 1: 修改 main.py**

Modify `Backend/src/main.py` 的 CORS 中间件块为：

```python
app.add_middleware(
    CORSMiddleware,
    # 本地演示固定 origin（前端 Vite 默认 0.0.0.0:5173，局域网访问经 192.168.137.1）
    allow_origins=["http://localhost:5173", "http://192.168.137.1:5173"],
    allow_methods=["*"],
    allow_headers=["*"],
)
```

（移除 `allow_credentials=True`——`*` 与 credentials 组合本身矛盾，固定 origin 后不再需要。）

- [ ] **Step 2: 验证**

Run:
```bash
cd E:/projects/DocuMind/Backend && uv run python -c "from main import app; cors = [m for m in app.user_middleware if m.cls.__name__=='CORSMiddleware'][0]; print(cors.kwargs['allow_origins'], cors.kwargs.get('allow_credentials'))"
```
Expected: `['http://localhost:5173', 'http://192.168.137.1:5173'] False`（或缺省 None 亦可）。

Run: `cd E:/projects/DocuMind/Backend && uv run pytest tests/ -v`
Expected: 42 passed（含 P1-2 新增）。

---

## 回归清单

| 任务 | 回归命令 |
|------|---------|
| P1-1 / P1-2 / P1-3 | `cd Backend && uv run pytest tests/ -v`（最终 42 passed） |
| 全链 | `cd Frontend && npm run build`（前端零改动，无需重跑，冒烟时确认问答正常） |

## 交付顺序

P1-1 → P1-2 → P1-3（互相独立）
